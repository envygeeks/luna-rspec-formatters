source "https://rubygems.org"
gemspec

group :development do

  unless ENV['CI']
    gem 'pry'
  end

  # --------------------------------------------------------------------------
  # Dependencies for people who use bundler.
  # --------------------------------------------------------------------------

  gem 'rake'
end
